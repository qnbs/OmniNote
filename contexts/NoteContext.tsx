/**
 * DEPRECATED
 * This context has been replaced by Redux Slices (features/notes/noteSlice.ts).
 * Keeping this file empty to ensure clean migration and prevent import errors if referenced legacy code exists.
 */
export {};